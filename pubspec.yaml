name: pharma_search
description: "pharma search is an app used to search project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.9.0
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  sqflite: ^2.3.3+1  # SQLite plugin
  path: ^1.9.0       # Path utilities
  path_provider: ^2.1.4
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/db.db